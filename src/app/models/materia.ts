export interface Materia {
    id: number;           // Identificador único de la materia
    nombre: string;       // Nombre de la materia
    docenteId: number;    // ID del docente asociado a la materia
}
